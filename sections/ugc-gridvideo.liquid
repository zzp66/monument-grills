<style>
  .ugc-gridvideo {
    padding: 60px 0;
    margin: 0;
    background-color: #d2e3ed;
  }
  .ugc-gridvideo video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .grid-video-content {
    margin-top: 40px;
  }
  .grid-video-box {
    column-count: 4;
    column-gap: 15px;
  }
  .video-card {
    break-inside: avoid;
    position: relative;
    cursor: pointer;
    margin-bottom: 15px;
    display: block;
  }
  .video-card img {
    width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
  }
  .video-card .video-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
  .video-card .play-btn {
    display: flex;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: rgba(0, 0, 0, 0.5);
    border-radius: 50%;
    padding: 12px;
  }
  .bottom-bar {
    width: 100%;
    position: absolute;
    bottom: 0;
    display: flex;
    align-items: center;
    padding: 8px 10px;
    background: linear-gradient(to top, #000000, #00000000);
    color: white;
    font-size: 14px;
  }
  .video-card img.ig-icon {
    max-width: 40px;
    height: auto;
  }
  .bottom-bar-text {
    margin-left: 8px;
    display: flex;
    flex-direction: column;
  }
  .bottom-bar-text span.text-strong {
    font-size: 16px;
    font-weight: 600;
  }

  .video-modal {
    display: none;
    position: fixed;
    z-index: 1000;
    inset: 0;
    background: rgba(0, 0, 0, 0.8);
    justify-content: center;
    align-items: center;
  }
  .modal-content {
    padding: 40px;
    background: none;
    max-width: 900px;
    width: 90%;
    position: relative;
  }
  .videomodal-close {
    position: absolute;
    top: 10px;
    right: 10px;
    font-size: 32px;
    color: white;
    cursor: pointer;
  }
  a.gridvideo-link {
    display: flex;
    justify-content: center;
    color: #333;
    margin-top: 40px;
  }

  a.gridvideo-link:hover {
    text-decoration: underline;
    color: #e55420;
  }

  @media (max-width: 767px) {
    .grid-video-content {
      margin-top: 30px;
    }
    .grid-video-box {
      column-count: 2;
      column-gap: 10px;
    }
    .video-card .play-btn {
      padding: 5px;
    }
    .video-card .play-btn svg {
      width: 20px;
      height: auto;
    }
    .video-card img.ig-icon {
      max-width: 20px;
    }
    .bottom-bar-text span.text-strong {
      font-size: 12px;
      line-height: 1;
    }
    .bottom-bar-text span.text-ins {
      font-size: 12px;
      line-height: 1;
    }
    a.gridvideo-link {
      margin-top: 30px;
    }
  }
</style>

<div class="ugc-gridvideo">
  <div class="container">
    <h2 class="section__heading mb-0 flex-auto text-center gridvideo-title">
      Get Monument Moments <br>
      The Everybody Everywhere Grill.
    </h2>
    <div class="grid-video-content">
      <div class="grid-video-box">
        {% for block in section.blocks %}
          <div class="video-card" data-video="{{ block.settings.video_url }}">
            <img src="{{ block.settings.thumbnail | image_url: master }}" alt="Video Thumbnail" class="thumbnail">
            <div class="video-overlay">
              <div class="play-btn">
                <svg width="40" height="40" viewBox="0 0 24 24" fill="white">
                  <path d="M8 5v14l11-7z"/>
                </svg>
              </div>
              <div class="bottom-bar">
                <img
                  src="https://cdn.shopify.com/s/files/1/1639/3511/files/shipin.png?v=1754701409"
                  alt="Instagram"
                  class="ig-icon"
                >
                <div class="bottom-bar-text">
                  <span class="text-strong">Monument Grills</span>
                  <span class="text-ins">Instagram</span>
                </div>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
      <a href="{{ section.settings.learn_more_link }}" class="gridvideo-link">Learn More ></a>
    </div>
  </div>
  <!-- Modal -->
  <div id="videoModal" class="video-modal">
    <div class="modal-content">
      <span class="videomodal-close">&times;</span>
      <div id="videoContainer"></div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const modal = document.getElementById('videoModal');
    const videoContainer = document.getElementById('videoContainer');
    const closeBtn = document.querySelector('.video-modal .videomodal-close');

    document.querySelectorAll('.video-card').forEach((card) => {
      card.addEventListener('click', () => {
        const videoUrl = card.getAttribute('data-video');
        let embed;
        if (videoUrl.includes('youtube.com') || videoUrl.includes('youtu.be')) {
          embed = `<iframe width="100%" height="100%" src="${videoUrl.replace(
            'watch?v=',
            'embed/'
          )}" frameborder="0" allowfullscreen></iframe>`;
        } else if (videoUrl.endsWith('.mp4')) {
          embed = `<video controls autoplay width="100%" height="100%"><source src="${videoUrl}" type="video/mp4"></video>`;
        } else {
          embed = `<iframe src="${videoUrl}" width="100%" height="100%" frameborder="0" allowfullscreen></iframe>`;
        }
        videoContainer.innerHTML = embed;
        modal.style.display = 'flex';
      });
    });

    closeBtn.addEventListener('click', () => {
      modal.style.display = 'none';
      videoContainer.innerHTML = '';
    });

    modal.addEventListener('click', (e) => {
      if (e.target === modal) {
        modal.style.display = 'none';
        videoContainer.innerHTML = '';
      }
    });
  });
</script>

{% schema %}
{
  "name": "UGC Grid Video",
  "settings": [
    {
      "type": "url",
      "id": "learn_more_link",
      "label": "Learn More Link"
    }
  ],
  "blocks": [
    {
      "type": "video_item",
      "name": "Video Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "thumbnail",
          "label": "Thumbnail Image"
        },
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL (MP4/YouTube/Instagram)"
        }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "UGC Grid Video"
    }
  ]
}
{% endschema %}
