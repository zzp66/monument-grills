<link rel="stylesheet" href="{{ 'footer.css' | asset_url }}" media="print" onload="this.media='all'">


{%- if section.settings.enable_country_selector or section.settings.enable_language_selector -%}
  <script src="{{ 'custom-select.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- style -%}
  .footer {
    --bg-color: {{ section.settings.bg_color.rgba }};
    --heading-color: {{ section.settings.text_color.rgb }};
    --text-color: {{ section.settings.text_color.rgb }};
  }
{%- endstyle -%}

{%- if section.settings.show_back_to_top -%}
  <a href="#" class="back-to-top text-sm block border-top font-bold text-center text-current p-6 border-bottom"{% if settings.animations_enabled != "disabled" %} data-cc-animate{% endif %}>{{ 'sections.footer.back_to_top' | t }}</a>
{%- endif -%}


<footer class="footer bg-theme-bg text-theme-text">
  <div class="container">
    {% if section.settings.show_community_link != blank %}
      <link rel="stylesheet" href="{{ 'text-overlay.css' | asset_url }}" media="print" onload="this.media='all'">
      {% style %}
        #{{ section.id }} .font-size{
          font-size: {{ section.settings.font_size }}px;
        }
        #{{ section.id }} .font-weight{
          font-weight: bold;
        }
        #{{ section.id }} .font-uppercase{
          text-transform:uppercase;
        }
        .community-warp .max-w-text-overlay{
          max-width:100%;
        }
        .community-warp p{
          /* white-space: nowrap; */
        }
        .no-show{
          display:none;
        }
        .show{
          display:block;
        }
        .community-btn{
          width:200px;
          font-size: 16px;
        }
        @media (max-width:850px){
         #{{ section.id }} .font-size{
            font-size: {{ section.settings.mobile_font_size }}px;
          }
          .community-warp p{
            white-space: wrap;
          }
        }
      {% endstyle %}

      {% liquid
        assign current_url = page.url | downcase
        if current_url contains 'monument-grills-day-2024' or current_url contains 'social-impact'
          assign show_community = 'show'
        else
          assign show_community = 'no-show'
        endif
      %}
      <div class="community-warp {{ show_community }}" >
        <div class="max-w-text-overlay {{ section.settings.community_text_position }} {{ section.settings.community_text_align }}" style="padding-top:{{ section.settings.padding_top }}px;padding-bottom:{{ section.settings.padding_bottom }}px;">
          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {% when 'community' %}
              
                <div class="hide" id="{% if section.settings.link_id != blank %}{{ section.settings.link_id }}{% endif %}"></div>
                
                {%- if block.settings.community_heading != blank -%}
                  <{% if block.settings.heading_h1 %}h1{% else %}h2{% endif %} class="{% if block.settings.title_weight != blank %}font-weight{% endif %} {% if block.settings.title_uppercase != blank %} font-uppercase {% endif %} section__block section__block--heading md-down:px-gutter {{ block.settings.heading_size }}" {{ block.shopify_attributes }} 
                    >
                    <span class="contained-heading inline-block">{{- block.settings.community_heading | escape -}}</span>
                  </{% if block.settings.heading_h1 %}h1{% else %}h2{% endif %}>
                {%- endif -%}

                {%- if block.settings.community_text != blank -%}
                  <div class="section__block reading-width--inline md-down:px-gutter rte{% if section.settings.text_align == 'text-start' %} mr-auto{% elsif section.settings.text_align == 'text-end' %} ml-auto{% else %} mx-auto{% endif %}{% if block.settings.enlarge_text %} enlarge-text{% endif %}" {{ block.shopify_attributes }}>
                    {{ block.settings.community_text }}
                  </div>
                {%- endif -%}

                {%- if block.settings.community_button != blank -%}
                  <div class="section__block section__block--buttons md-down:px-gutter inline-flex flex-wrap {% if section.settings.text_align == 'text-start' %} justify-start{% elsif section.settings.text_align == 'text-end' %} justify-end{% else %} justify-center{% endif %}{% if section.settings.mob_center_text %} md-down:center{% endif %}" {{ block.shopify_attributes }}>
                    {%- if block.settings.community_button != blank %}
                      <a class="btn community-btn {{ block.settings.community_style }}" {% if block.settings.community_link == blank %}role="link" aria-disabled="true"{% else %}href="{{ block.settings.community_link }}"{% endif %}>
                        {{- block.settings.community_button | escape -}}
                      </a>
                    {%- endif -%}
                  </div>
                {%- endif -%}
            {% endcase %}
          {% endfor %}
        </div>
      </div>
    {% endif %}
    
    {%- if section.blocks.size > 0 -%}
      <div class="footer__main md:flex md:flex-wrap mb-10 md:mb-6">
        {%- assign is_first_disclosure = true -%}
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'text' -%}
              <div class="footer-col footer-col--text footer-col--not-collapsed{% if section.blocks.size == 1 %} footer-col--text-full-width{% endif %}" {{ block.shopify_attributes }}{% if settings.animations_enabled != "disabled" %} data-cc-animate data-cc-animate-delay="{{ forloop.index | times: 0.07 }}s"{% endif %}>
                <div class="footer-block mb-8 {{ block.settings.text_align }}">
                  {%- if block.settings.image != blank -%}
                    <div class="media relative" style="max-width: {{ block.settings.image_width }}px;">
                      {% render 'image', class: "footer-block__image", image: block.settings.image, src_width: block.settings.image_width, srcset_2x: true %}
                    </div>
                  {%- endif -%}
                  {%- if block.settings.heading != blank -%}
                    <h2 class="footer-block__heading font-body font-bold text-h6 regular-text">{{ block.settings.heading | escape }}</h2>
                  {%- endif -%}
                  {%- if block.settings.text != blank -%}
                    <div class="footer-block__text rte">
                      {{ block.settings.text }}
                    </div>
                  {%- endif -%}
                  {%- if block.settings.button_label != blank -%}
                    <div class="footer-block__btns">
                      <a class="btn btn--primary" {% if block.settings.button_link == blank %}role="link" aria-disabled="true"{% else %}href="{{ block.settings.button_link }}"{% endif %}>
                        {{- block.settings.button_label | escape -}}
                      </a>
                    </div>
                  {%- endif -%}
                  {%- if shop.features.follow_on_shop? and block.settings.enable_follow_on_shop -%}
                    <div class="footer-block__btns">
                      {% # theme-check-disable %}
                      {{ shop | login_button: action: 'follow' }}
                      {% # theme-check-enable %}
                    </div>
                  {%- endif -%}
                  
                </div>
              </div>

            {%- when 'newsletter' -%}
              <div class="footer-col footer-col--newsletter{% if block.settings.collapse_mobile %} footer-col--collapsed{% else %} footer-col--not-collapsed{% endif %}" {{ block.shopify_attributes }}{% if settings.animations_enabled != "disabled" %} data-cc-animate data-cc-animate-delay="{{ forloop.index | times: 0.07 }}s"{% endif %}>
                {%- if block.settings.collapse_mobile -%}
                  <footer-menu>
                    <details class="footer-menu disclosure{% if is_first_disclosure %} footer-menu--first{% endif %}" open>
                      <summary tabindex="-1">
                        <div class="flex justify-between items-center">
                          <h2 class="disclosure__title font-body font-bold text-h6 regular-text">
                            {{ block.settings.heading | escape }}
                          </h2>
                          <span class="disclosure__toggle">
                            {%- if settings.disclosure_toggle == 'plus' -%}
                              {% render 'icon-plus' %}
                            {%- else -%}
                              {% render 'icon-chevron-down' %}
                            {%- endif -%}
                          </span>
                        </div>
                      </summary>
                      <div class="disclosure__panel has-motion">
                        <div class="disclosure__content">
                          {%- if block.settings.text != blank -%}
                            {{ block.settings.text }}
                          {%- endif -%}

                          {% render 'newsletter-signup', id: 'footer-signup', arrow_button: true, tags: 'newsletter' %}
                        </div>
                      </div>
                    </details>
                    {%- if block.settings.show_social -%}
                      {%- liquid
                        if block.settings.text_align == "text-end"
                          assign class = "justify-end"
                        elsif block.settings.text_align == "text-center"
                          assign class = "justify-center"
                        else
                          assign class = "justify-start"
                        endif
                      -%}
                      <div class="footer-block__btns footer-block__social">
                        {% render 'social-media', icon: settings.social_custom_icon, icon_height: 24, class: class %}
                      </div>
                    {%- endif -%}
                  </footer-menu>
                  {%- assign is_first_disclosure = false -%}
                {%- else -%}
                  <div class="footer-block">
                    <h2 class="footer-block__heading font-body font-bold text-h6 regular-text">
                      {%- if block.settings.heading != blank -%}
                        {{ block.settings.heading | escape }}
                      {%- endif -%}
                    </h2>

                    {%- if block.settings.text != blank -%}
                      {{ block.settings.text }}
                    {%- endif -%}

                    {% render 'newsletter-signup', id: 'footer-signup', arrow_button: true, tags: 'newsletter' %}
                  </div>
                {%- endif -%}
              </div>

            {%- when 'link_list' -%}
              {%- if block.settings.menu != blank and block.settings.heading != blank -%}
                <div class="footer-col footer-col--links footer-col--collapsed" {{ block.shopify_attributes }}{% if settings.animations_enabled != "disabled" %} data-cc-animate data-cc-animate-delay="{{ forloop.index | times: 0.07 }}s"{% endif %}>
                  <footer-menu>
                    <details class="footer-menu disclosure{% if is_first_disclosure %} footer-menu--first{% endif %}" open>
                      <summary tabindex="-1">
                        <div class="flex justify-between items-center">
                          <h2 class="disclosure__title font-body font-bold text-h6 regular-text">
                            {{- block.settings.heading | escape -}}
                          </h2>
                          <span class="disclosure__toggle">
                            {%- if settings.disclosure_toggle == 'plus' -%}
                              {% render 'icon-plus' %}
                            {%- else -%}
                              {% render 'icon-chevron-down' %}
                            {%- endif -%}
                          </span>
                        </div>
                      </summary>
                      <div class="disclosure__panel has-motion">
                        <ul class="footer-menu__links disclosure__content" role="list">
                          {%- for link in block.settings.menu.links -%}
                            {% if link.url contains 'privacy-policy' 
                               or link.url contains 'shipping-policy' 
                               or link.url contains 'refund-policy' 
                               or link.url contains 'faq' 
                               or link.url contains 'register-grill' 
                               or link.url contains 'aftership' 
                               or link.url contains 'sponsorship-application' 
                               or link.url contains 'perfect-product-finder' 
                               or link.url contains 'monumentgrills-enterprise-reseller-application' 
                               or link.url contains 'feedback'
                               or link.url contains 'warranty-policy'
                            %}
                              <li class="{% if link.title =='Membership' %} fmenu__membership {% endif %}">
                                <a href="{{ link.url }}" rel="nofollow">{{ link.title }}</a>
                              </li>
                            {% else %}
                              <li class="{% if link.title =='Membership' %} fmenu__membership {% endif %}">
                                <a href="{{ link.url }}">{{ link.title }}</a>
                              </li>
                            {% endif %}
                          {%- endfor -%}
                        </ul>
                      </div>
                    </details>
                  </footer-menu>
                </div>
                {%- assign is_first_disclosure = false -%}
              {%- endif -%}
          {%- endcase -%}
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- if section.settings.enable_country_selector or section.settings.enable_language_selector or section.settings.show_payment_icons -%}
      <div class="footer__meta grid grid-cols-1 lg:grid-cols-2 gap-x-theme gap-y-10"{% if settings.animations_enabled != "disabled" %} data-cc-animate{% endif %}>
        {%- if section.settings.show_payment_icons -%}
          <div class="footer__payment{% if section.settings.enable_country_selector or section.settings.enable_language_selector %} lg:col-start-2 lg:row-start-1 footer__payment--right{% endif %}">
            <span class="visually-hidden">{{ 'sections.footer.payment_methods' | t }}</span>
            <ul class="payment-icons flex flex-wrap" role="list">
              {%- for type in shop.enabled_payment_types -%}
                <li>{{ type | payment_type_svg_tag: class: 'payment-icon' }}</li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}

        {%- if section.settings.enable_country_selector or section.settings.enable_language_selector -%}
          <div class="lg:col-start-1 lg:row-start-1 footer__localization">
            {% render 'localization-form', id: 'footer-localization', hide_labels: true %}
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- if section.blocks.size > 0 or section.settings.enable_country_selector or section.settings.enable_language_selector or section.settings.show_payment_icons -%}
    <hr class="footer__hr"{% if settings.animations_enabled != "disabled" %} data-cc-animate{% endif %}>
    {% endif %}

    <div class="footer__base grid grid-cols-1 lg:grid-cols-2 gap-x-theme gap-y-6 text-xs"{% if settings.animations_enabled != "disabled" %} data-cc-animate{% endif %}>
      <div class="powered-by">
        <span>&copy; {{ "now" | date: "%Y" }} {{ shop.name | link_to: routes.root_url }}.</span>
        {% if section.settings.show_powered_by %}
          <span>{{ powered_by_link }}</span>
        {% endif %}
      </div>
      {%- if section.settings.secondary_menu != blank -%}
        <div class="other-links">
          <ul class="footer-sublinks flex flex-wrap gap-x-theme" role="list">
            {% for link in section.settings.secondary_menu.links %}
              <li class="footer-sublinks__item">
                <a href="{{ link.url }}">{{ link.title }}</a>
              </li>
            {% endfor %}
          </ul>
        </div>
      {%- endif -%}
    </div>
  </div>
</footer>

{% javascript %}
  customElements.whenDefined('details-disclosure').then(() => {
    class FooterMenu extends DetailsDisclosure {
      constructor() {
        super();
        this.reset(false);
        window.addEventListener('on:breakpoint-change', this.reset.bind(this));
      }

      reset() {
        const isLargeScreen = window.matchMedia(theme.mediaQueries.md).matches;
        const isConfirmNewsletter = !isLargeScreen &&
          (window.location.hash === "#footer-signup_form" && this.querySelector('#footer-signup_form') !== null);

        this.disclosure.open = isLargeScreen || isConfirmNewsletter;

        if (isLargeScreen) {
          this.toggle.setAttribute('tabindex', '-1');
        } else {
          this.toggle.removeAttribute('tabindex');
        }
      }
    }

    customElements.define('footer-menu', FooterMenu);
  });
{% endjavascript %}

{% schema %}
{
  "name": "Footer -- wenfei",
  "class": "cc-footer",
  "settings": [
    {
      "type": "header",
      "content": "跳转ID设置"
    },
    {
      "type": "text",
      "id": "link_id",
      "label": "Link ID"
    },
        
    {
      "type": "checkbox",
      "id": "show_back_to_top",
      "label": "Show 'Back to top'",
      "default": true
    },
    {
      "type": "header",
      "content": "Community Settings"
    },
    {
      "type": "checkbox",
      "id": "show_community_link",
      "label": "Show 'Community'",
      "default": false
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Padding Top",
      "default": 10
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Padding Button",
      "default": 10
    },
    {
      "type": "select",
      "id": "community_text_position",
      "label": "Community Text position",
      "options": [
        {
          "value": "justify-start",
          "label": "Left"
        },
        {
          "value": "justify-center",
          "label": "Center"
        },
        {
          "value": "justify-end",
          "label": "Right"
        }
      ],
      "default": "justify-center"
    },
    {
      "type": "select",
      "id": "community_text_align",
      "label": "Community Text alignment",
      "options": [
        {
          "value": "text-start",
          "label": "Left"
        },
        {
          "value": "text-center",
          "label": "Center"
        },
        {
          "value": "text-end",
          "label": "Right"
        }
      ],
      "default": "text-center"
    },
    {
      "type": "header",
      "content": "Payment Settings"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "Show payment icons",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_powered_by",
      "label": "Show 'Powered by Shopify'",
      "default": true
    },
    {
      "type": "header",
      "content": "Country/Region selector",
      "info": "To add a country/region, go to your [payment settings](/admin/settings/payments)"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "label": "Enable country/region selector",
      "default": true
    },
    {
      "type": "header",
      "content": "Language selector",
      "info": "To add a language, go to your [language settings](/admin/settings/languages)"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "label": "Enable language selector",
      "default": true
    },
    {
      "type": "header",
      "content": "Secondary menu"
    },
    {
      "type": "link_list",
      "id": "secondary_menu",
      "label": "Secondary menu",
      "info": "This menu won't show dropdown items."
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#f4f4f4"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#070707"
    }
  ],
  "max_blocks": 6,
  "blocks": [
    {
      "type": "community",
      "name":"Community",
      "settings": [
        
        {
          "type": "text",
          "id": "community_heading",
          "label": "Heading",
          "default": "Rich text headline"
        },
        {
          "type": "checkbox",
          "id": "heading_h1",
          "label": "Set as page heading (uses h1 tag)",
          "info": "For optimum SEO, a page should only have one h1 and it should be the first heading on the page.",
          "default": false
        },
        {
          "type": "range",
          "id": "font_size",
          "min": 16,
          "max": 160,
          "step": 16,
          "unit": "px",
          "label": "PC Font Size",
          "default": 64
        },
        {
          "type": "range",
          "id": "mobile_font_size",
          "min": 16,
          "max": 64,
          "step": 16,
          "unit": "px",
          "label": "Mobile Font Size",
          "default": 32
        },
        {
          "type": "checkbox",
          "id": "title_weight",
          "label": "Font Weight",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "title_uppercase",
          "label": "Font Uppercase",
          "default": false
        },
        
        {
          "type": "richtext",
          "id": "community_text",
          "label": "Text",
          "default": "<p>Share information about your store with your customers. Describe a product, share announcements, or welcome customers to your store.</p>"
        },
        {
          "type": "checkbox",
          "id": "enlarge_text",
          "label": "Enlarge text",
          "default": true
        },
         {
          "type": "text",
          "id": "community_button",
          "label": "Community Button",
          "default": "JOIN US"
        },
        {
          "type": "url",
          "id": "community_link",
          "label": "Community link"
        },
        {
          "type": "select",
          "id": "community_style",
          "label": "Community Button style",
          "options": [
            {
              "value": "link",
              "label": "Link"
            },
            {
              "value": "btn--primary",
              "label": "Primary button"
            },
            {
              "value": "btn--secondary",
              "label": "Secondary button"
            }
          ],
          "default": "btn--primary"
        }
      ]
    },
    {
      "type": "link_list",
      "name": "Footer menu",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading (required)",
          "default": "Footer links"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu",
          "info": "This menu won't show dropdown items.",
          "default": "footer"
        }
      ]
    },
    {
      "type": "text",
      "name": "Image, Text and Socials",
      "limit": 3,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "range",
          "id": "image_width",
          "min": 50,
          "max": 300,
          "step": 10,
          "unit": "px",
          "label": "Maximum image width",
          "default": 150
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Talk about your store"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Share store details, promotions, or brand content with your customers.</p>"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        },
        {
          "type": "checkbox",
          "id": "enable_follow_on_shop",
          "default": false,
          "label": "Enable Follow on Shop",
          "info": "To allow customers to follow your store on the Shop app from your storefront, Shop Pay must be enabled. [Learn more](https://help.shopify.com/manual/online-store/themes/customizing-themes/follow-on-shop)"
        },
        
        {
          "type": "select",
          "id": "text_align",
          "label": "Content alignment",
          "options": [
            {
              "value": "text-start",
              "label": "Left"
            },
            {
              "value": "text-center",
              "label": "Center"
            },
            {
              "value": "text-end",
              "label": "Right"
            }
          ],
          "default": "text-start"
        }
      ]
    },
    {
      "type": "newsletter",
      "name": "Email signup",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_social",
          "label": "Show social media links",
          "info": "Add links to your social media accounts in theme settings.",
          "default": false
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Newsletter"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Sign up for exclusive offers, original stories, events and more.</p>"
        },
        {
          "type": "paragraph",
          "content": "Customers who subscribe will have their email address added to the \"accepts marketing\" [customer list](/admin/customers?query=&accepts_marketing=1)"
        },
        {
          "type": "checkbox",
          "id": "collapse_mobile",
          "label": "Show as collapsible content on mobile",
          "default": true
        }
      ]
    }
  ]
}
{% endschema %}
